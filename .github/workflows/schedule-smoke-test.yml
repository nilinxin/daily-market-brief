name: Temporary Schedule Delivery Test

on:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write

jobs:
  send:
    uses: ./.github/workflows/daily-market-brief.yml
    with:
      mode: cn_pm_preopen
    secrets: inherit
