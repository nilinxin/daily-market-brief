name: Scheduled A-Share Midday Brief

on:
  schedule:
    # Beijing 11:40, Monday-Friday.
    - cron: "40 3 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  send:
    uses: ./.github/workflows/daily-market-brief.yml
    with:
      mode: cn_midday_close
    secrets: inherit
