name: Scheduled A-Share Midday Brief

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  send:
    uses: ./.github/workflows/daily-market-brief.yml
    with:
      mode: cn_midday_close
    secrets: inherit
