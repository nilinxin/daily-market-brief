name: Scheduled A-Share PM Preopen Brief

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  send:
    uses: ./.github/workflows/daily-market-brief.yml
    with:
      mode: cn_pm_preopen
    secrets: inherit
