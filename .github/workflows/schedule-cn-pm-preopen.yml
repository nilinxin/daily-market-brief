name: Scheduled A-Share PM Preopen Brief

on:
  schedule:
    # Beijing 12:50, Monday-Friday.
    - cron: "50 4 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  send:
    uses: ./.github/workflows/daily-market-brief.yml
    with:
      mode: cn_pm_preopen
    secrets: inherit
